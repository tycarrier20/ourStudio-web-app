import React from "react";

export default React.createContext();

// CONTEXT SYSTEM REACT - Utilized to communicate from a parent component to any arbitrary child component without
// the need for props.
